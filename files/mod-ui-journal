#!/bin/bash
# Follow the mod-ui service journal.
# Defaults to following (-f); pass any args through to journalctl.
if [ $# -eq 0 ]; then
    exec sudo journalctl -f -u mod-ui
else
    exec sudo journalctl -u mod-ui "$@"
fi
